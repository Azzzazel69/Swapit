// This file is no longer used.
// Firebase configuration for web push notifications was part of the PWA setup.
// Now, push notifications are handled natively through the Capacitor Push Notifications plugin,
// which uses a different configuration method (google-services.json for Android, and Xcode setup for iOS).
